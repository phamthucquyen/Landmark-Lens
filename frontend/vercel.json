{
  "buildCommand": "flutter build web --release --web-renderer canvaskit --dart-define=API_URL=$API_URL",
  "outputDirectory": "build/web",
  "installCommand": "if [ ! -d \"/usr/local/flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1 /usr/local/flutter; fi && export PATH=\"$PATH:/usr/local/flutter/bin\" && flutter doctor && flutter pub get",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
